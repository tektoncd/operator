# List components that the operator fetches payload from
tekton-pipeline:
  version: nightly
  internal: pipeline
  targets:
  - name: release
tekton-trigger:
  version: nightly
  internal: triggers
  targets:
  - name: release
  - name: interceptors
tekton-dasbhoard:
  version: nightly
  internal: dashboard
  platforms: [ "kubernetes" ]
  targets:
  - name: release
    prefix: tekton-dashboard-fullaccess
    url: https://storage.googleapis.com/tekton-releases-nightly/dashboard/latest/tekton-dashboard-release.yaml
  - name: release-readonly
    prefix: tekton-dashboard-readonly
    url: https://storage.googleapis.com/tekton-releases-nightly/dashboard/latest/tekton-dashboard-release-readonly.yaml
tekton-results:
  version: v0.4.0
  internal: results
  platforms: [ "kubernetes" ]
  targets:
  - name: release
tekton-chains:
  version: nightly
  internal: chains
  targets:
  - name: release
tekton-addon/pipelines-as-code:
  version: 0.5.3
  targets:
  - name: release
    url: https://raw.githubusercontent.com/openshift-pipelines/pipelines-as-code/release-0.5.3/release-0.5.3.yaml
